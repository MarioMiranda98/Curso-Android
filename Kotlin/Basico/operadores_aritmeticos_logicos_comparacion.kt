fun main(args: Array<String>) {
    //+ - / *
    //< > == <= >=
    // ! && ||
    var a  = 8
    var b = 8
    var datoBooleano = a != b

    println(datoBooleano)
    println(a != b && c < a)
    println(a != b || c < a)
    println((a != b || c < a) && (b == c))
}